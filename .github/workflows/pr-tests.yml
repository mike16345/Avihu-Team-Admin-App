name: PR Tests

on:
  pull_request:
    branches:
      - devel
    types:
      - opened
      - synchronize
      - reopened
      - ready_for_review

concurrency:
  group: pr-tests-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
      actions: write
    defaults:
      run:
        working-directory: frontend

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run tests and build readable report
        id: test_report
        continue-on-error: true
        run: npm run test:report

      - name: Add readable test summary
        if: always()
        working-directory: .
        run: |
          if [ -f ".test-report/summary.md" ]; then
            cat ".test-report/summary.md" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "Test reporting failed before a summary could be generated." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload readable test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-report-${{ github.run_id }}
          path: .test-report/
          if-no-files-found: warn

      - name: Check failure-email credentials
        id: smtp_credentials
        if: always() && steps.test_report.outcome == 'failure'
        working-directory: .
        env:
          TEST_REPORT_SMTP_USER: ${{ secrets.TEST_REPORT_SMTP_USER }}
          TEST_REPORT_SMTP_APP_PASSWORD: ${{ secrets.TEST_REPORT_SMTP_APP_PASSWORD }}
        run: |
          if [ -n "$TEST_REPORT_SMTP_USER" ] && [ -n "$TEST_REPORT_SMTP_APP_PASSWORD" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Email failed test report
        id: send_failure_email
        if: always() && steps.test_report.outcome == 'failure' && steps.smtp_credentials.outputs.available == 'true'
        continue-on-error: true
        working-directory: .
        env:
          TEST_REPORT_SMTP_USER: ${{ secrets.TEST_REPORT_SMTP_USER }}
          TEST_REPORT_SMTP_APP_PASSWORD: ${{ secrets.TEST_REPORT_SMTP_APP_PASSWORD }}
        run: |
          curl --fail-with-body --silent --show-error \
            --url "smtps://smtp.gmail.com:465" --ssl-reqd \
            --mail-from "${TEST_REPORT_SMTP_USER}" \
            --mail-rcpt "michaelgani815@gmail.com" \
            --user "${TEST_REPORT_SMTP_USER}:${TEST_REPORT_SMTP_APP_PASSWORD}" \
            --upload-file ".test-report/email.eml"

      - name: Record missing failure-email credentials
        if: always() && steps.test_report.outcome == 'failure' && steps.smtp_credentials.outputs.available != 'true'
        working-directory: .
        run: echo "Failure email skipped because SMTP secrets are unavailable." >> "$GITHUB_STEP_SUMMARY"

      - name: Record failure-email delivery error
        if: always() && steps.test_report.outcome == 'failure' && steps.send_failure_email.outcome == 'failure'
        working-directory: .
        run: echo "Failure email delivery failed; the test report artifact is still available." >> "$GITHUB_STEP_SUMMARY"

      - name: Preserve failed test status
        if: always() && steps.test_report.outcome == 'failure'
        working-directory: .
        run: exit 1
